# App URL (for OAuth redirect and public image URLs). No trailing slash.
# Local dev:
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Production (Vercel): set in Vercel env to e.g. https://automation-aditya.vercel.app

# Meta / Facebook (Instagram Business API)
# Create app at https://developers.facebook.com/apps/
# Add "Instagram Graph API" and "Facebook Login"
# Use case: Business → Instagram
META_APP_ID=your_app_id
META_APP_SECRET=your_app_secret

# Caption + hashtag generation (try OpenAI first; if missing or failing, uses Gemini)
# Must be in project root. After changing, restart dev server: npm run dev
OPENAI_API_KEY=

# Gemini (fallback when OPENAI_API_KEY is not set or fails). Get key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Redis (optional; for BullMQ scheduled posts)
# If not set, scheduling may not run until you add Redis
REDIS_URL=redis://localhost:6379

# Google Drive (optional; pick images from Drive instead of uploading)
# Create OAuth 2.0 credentials at https://console.cloud.google.com/apis/credentials
# Redirect URI = the URL you use to open the app + /api/drive/callback (no trailing slash).
# Visit /api/drive/redirect-uri in the browser to get the exact URI to add. Enable Google Drive API.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Auth (multi-user: sign up with email/password; each user has separate data)
# AUTH_SECRET: secret for signing session cookie (optional; uses fallback if unset)
AUTH_SECRET=
AUTH_PASSWORD=

# Supabase (optional; when set, all app data is stored in Supabase instead of local files)
# Create a project at https://supabase.com → Settings → API: use URL and service_role key.
# Run the SQL in supabase/migrations/ in the Supabase SQL Editor to create tables.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
